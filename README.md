# Z-SCORE / NC Dashboard

La app está en [`app-gauss/`](app-gauss/README.md). Ahí están las instrucciones para correrla y publicarla en Render.

No subas `credentials.json` ni `.streamlit/secrets.toml`.
